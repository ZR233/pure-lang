---
name: task-fixture-rust
description: Apply the Task live fixture's dependency-free Rust implementation and verification constraints.
---

# Task fixture Rust workflow

Read `AGENTS.md` and `docs/product-contract.md`, keep executor scopes disjoint, avoid new
dependencies, and use `cargo test` plus the fixture verifier as the acceptance evidence.
