---
name: workflow-fixture-rust
description: Apply the workflow live fixture's dependency-free Rust implementation and verification constraints.
---

# Workflow fixture Rust

Read `AGENTS.md` and `docs/product-contract.md`, keep changes focused, avoid new dependencies,
and use `cargo test` plus the fixture verifier as acceptance evidence.
