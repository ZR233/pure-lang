// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;

import 'thread_stream.dart';
part 'attachment.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`

@freezed
sealed class BridgeAttachmentAdmissionContext
    with _$BridgeAttachmentAdmissionContext {
  const BridgeAttachmentAdmissionContext._();

  const factory BridgeAttachmentAdmissionContext.existingThread({
    required String threadId,
  }) = BridgeAttachmentAdmissionContext_ExistingThread;
  const factory BridgeAttachmentAdmissionContext.newThread({
    required BridgeThreadMode mode,
  }) = BridgeAttachmentAdmissionContext_NewThread;
}

class BridgeAttachmentDraft {
  final String draftId;
  final BridgeAttachmentModality modality;
  final String mediaType;
  final String filename;
  final BigInt byteSize;
  final int? width;
  final int? height;

  const BridgeAttachmentDraft({
    required this.draftId,
    required this.modality,
    required this.mediaType,
    required this.filename,
    required this.byteSize,
    this.width,
    this.height,
  });

  @override
  int get hashCode =>
      draftId.hashCode ^
      modality.hashCode ^
      mediaType.hashCode ^
      filename.hashCode ^
      byteSize.hashCode ^
      width.hashCode ^
      height.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeAttachmentDraft &&
          runtimeType == other.runtimeType &&
          draftId == other.draftId &&
          modality == other.modality &&
          mediaType == other.mediaType &&
          filename == other.filename &&
          byteSize == other.byteSize &&
          width == other.width &&
          height == other.height;
}

@freezed
sealed class BridgeAttachmentDraftSource with _$BridgeAttachmentDraftSource {
  const BridgeAttachmentDraftSource._();

  const factory BridgeAttachmentDraftSource.localFile({required String path}) =
      BridgeAttachmentDraftSource_LocalFile;
  const factory BridgeAttachmentDraftSource.remoteUrl({
    required String url,
    String? filename,
  }) = BridgeAttachmentDraftSource_RemoteUrl;
}

enum BridgeAttachmentModality { image, video, file }

class BridgeStudioPromptInput {
  final String text;
  final List<String> attachmentDraftIds;

  const BridgeStudioPromptInput({
    required this.text,
    required this.attachmentDraftIds,
  });

  @override
  int get hashCode => text.hashCode ^ attachmentDraftIds.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeStudioPromptInput &&
          runtimeType == other.runtimeType &&
          text == other.text &&
          attachmentDraftIds == other.attachmentDraftIds;
}
