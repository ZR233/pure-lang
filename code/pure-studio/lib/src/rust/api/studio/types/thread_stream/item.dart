// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../../../frb_generated.dart';
import '../attachment.dart';
import '../thread_stream.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'item.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

@freezed
sealed class BridgeSkillActivationCause with _$BridgeSkillActivationCause {
  const BridgeSkillActivationCause._();

  const factory BridgeSkillActivationCause.tool({required String toolCallId}) =
      BridgeSkillActivationCause_Tool;
  const factory BridgeSkillActivationCause.userGesture({
    required String invocationId,
  }) = BridgeSkillActivationCause_UserGesture;
}

@freezed
sealed class BridgeSkillResourceBase with _$BridgeSkillResourceBase {
  const BridgeSkillResourceBase._();

  const factory BridgeSkillResourceBase.directory({required String path}) =
      BridgeSkillResourceBase_Directory;
  const factory BridgeSkillResourceBase.url({required String url}) =
      BridgeSkillResourceBase_Url;
  const factory BridgeSkillResourceBase.opaque({required String description}) =
      BridgeSkillResourceBase_Opaque;
}

class BridgeThreadAgentIdentity {
  final String id;
  final String path;
  final String? parentPath;
  final String role;
  final String task;
  final int depth;

  const BridgeThreadAgentIdentity({
    required this.id,
    required this.path,
    this.parentPath,
    required this.role,
    required this.task,
    required this.depth,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      path.hashCode ^
      parentPath.hashCode ^
      role.hashCode ^
      task.hashCode ^
      depth.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadAgentIdentity &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          path == other.path &&
          parentPath == other.parentPath &&
          role == other.role &&
          task == other.task &&
          depth == other.depth;
}

@freezed
sealed class BridgeThreadAgentState with _$BridgeThreadAgentState {
  const BridgeThreadAgentState._();

  const factory BridgeThreadAgentState.queued() = BridgeThreadAgentState_Queued;
  const factory BridgeThreadAgentState.running() =
      BridgeThreadAgentState_Running;
  const factory BridgeThreadAgentState.succeeded({
    required PlatformInt64 completedAt,
    required String summary,
  }) = BridgeThreadAgentState_Succeeded;
  const factory BridgeThreadAgentState.denied({
    required PlatformInt64 deniedAt,
    required String reason,
  }) = BridgeThreadAgentState_Denied;
  const factory BridgeThreadAgentState.cancelled({
    required PlatformInt64 cancelledAt,
    required String reason,
  }) = BridgeThreadAgentState_Cancelled;
  const factory BridgeThreadAgentState.failed({
    required PlatformInt64 failedAt,
    required String error,
  }) = BridgeThreadAgentState_Failed;
}

class BridgeThreadAttachment {
  final String id;
  final BridgeAttachmentModality modality;
  final String mediaType;
  final String? filename;
  final int? width;
  final int? height;
  final BigInt byteSize;

  const BridgeThreadAttachment({
    required this.id,
    required this.modality,
    required this.mediaType,
    this.filename,
    this.width,
    this.height,
    required this.byteSize,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      modality.hashCode ^
      mediaType.hashCode ^
      filename.hashCode ^
      width.hashCode ^
      height.hashCode ^
      byteSize.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadAttachment &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          modality == other.modality &&
          mediaType == other.mediaType &&
          filename == other.filename &&
          width == other.width &&
          height == other.height &&
          byteSize == other.byteSize;
}

@freezed
sealed class BridgeThreadContentLifecycle with _$BridgeThreadContentLifecycle {
  const BridgeThreadContentLifecycle._();

  const factory BridgeThreadContentLifecycle.streaming() =
      BridgeThreadContentLifecycle_Streaming;
  const factory BridgeThreadContentLifecycle.completed({
    required PlatformInt64 completedAt,
  }) = BridgeThreadContentLifecycle_Completed;
  const factory BridgeThreadContentLifecycle.failed({
    required PlatformInt64 failedAt,
    required String error,
  }) = BridgeThreadContentLifecycle_Failed;
  const factory BridgeThreadContentLifecycle.cancelled({
    required PlatformInt64 cancelledAt,
    required String reason,
  }) = BridgeThreadContentLifecycle_Cancelled;
}

@freezed
sealed class BridgeThreadInferenceState with _$BridgeThreadInferenceState {
  const BridgeThreadInferenceState._();

  const factory BridgeThreadInferenceState.running() =
      BridgeThreadInferenceState_Running;
  const factory BridgeThreadInferenceState.completed({
    required PlatformInt64 completedAt,
    required BridgeTokenUsageSnapshot usage,
  }) = BridgeThreadInferenceState_Completed;
  const factory BridgeThreadInferenceState.failed({
    required PlatformInt64 failedAt,
    required String error,
  }) = BridgeThreadInferenceState_Failed;
  const factory BridgeThreadInferenceState.cancelled({
    required PlatformInt64 cancelledAt,
    required String reason,
  }) = BridgeThreadInferenceState_Cancelled;
}

class BridgeThreadItem {
  final String id;
  final String threadId;
  final String turnId;
  final BigInt ordinal;
  final BigInt revision;
  final PlatformInt64 createdAt;
  final PlatformInt64 updatedAt;
  final BridgeThreadItemState state;

  const BridgeThreadItem({
    required this.id,
    required this.threadId,
    required this.turnId,
    required this.ordinal,
    required this.revision,
    required this.createdAt,
    required this.updatedAt,
    required this.state,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      threadId.hashCode ^
      turnId.hashCode ^
      ordinal.hashCode ^
      revision.hashCode ^
      createdAt.hashCode ^
      updatedAt.hashCode ^
      state.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadItem &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          threadId == other.threadId &&
          turnId == other.turnId &&
          ordinal == other.ordinal &&
          revision == other.revision &&
          createdAt == other.createdAt &&
          updatedAt == other.updatedAt &&
          state == other.state;
}

class BridgeThreadItemDelta {
  final String itemId;
  final BigInt revision;
  final BridgeThreadItemDeltaState delta;

  const BridgeThreadItemDelta({
    required this.itemId,
    required this.revision,
    required this.delta,
  });

  @override
  int get hashCode => itemId.hashCode ^ revision.hashCode ^ delta.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadItemDelta &&
          runtimeType == other.runtimeType &&
          itemId == other.itemId &&
          revision == other.revision &&
          delta == other.delta;
}

@freezed
sealed class BridgeThreadItemDeltaState with _$BridgeThreadItemDeltaState {
  const BridgeThreadItemDeltaState._();

  const factory BridgeThreadItemDeltaState.text({required String delta}) =
      BridgeThreadItemDeltaState_Text;
  const factory BridgeThreadItemDeltaState.thinkingSummary({
    required int chunkIndex,
    required String delta,
  }) = BridgeThreadItemDeltaState_ThinkingSummary;
  const factory BridgeThreadItemDeltaState.thinkingContent({
    required int chunkIndex,
    required String delta,
  }) = BridgeThreadItemDeltaState_ThinkingContent;
  const factory BridgeThreadItemDeltaState.toolArguments({
    required String delta,
  }) = BridgeThreadItemDeltaState_ToolArguments;
  const factory BridgeThreadItemDeltaState.toolResult({required String delta}) =
      BridgeThreadItemDeltaState_ToolResult;
}

@freezed
sealed class BridgeThreadItemState with _$BridgeThreadItemState {
  const BridgeThreadItemState._();

  const factory BridgeThreadItemState.text({
    required BridgeThreadTextChannel channel,
    required String text,
    required List<BridgeThreadAttachment> attachments,
    required BridgeThreadContentLifecycle lifecycle,
  }) = BridgeThreadItemState_Text;
  const factory BridgeThreadItemState.thinking({
    required List<String> summary,
    required List<String> content,
    required BridgeThreadContentLifecycle lifecycle,
  }) = BridgeThreadItemState_Thinking;
  const factory BridgeThreadItemState.tool({
    required BridgeThreadToolInvocation invocation,
    required BridgeThreadToolState state,
  }) = BridgeThreadItemState_Tool;
  const factory BridgeThreadItemState.agent({
    required BridgeThreadAgentIdentity identity,
    required BridgeThreadAgentState state,
  }) = BridgeThreadItemState_Agent;
  const factory BridgeThreadItemState.turn({required BridgeTurnState state}) =
      BridgeThreadItemState_Turn;
  const factory BridgeThreadItemState.inference({
    required String inferenceId,
    required String model,
    required BridgeThreadInferenceState state,
  }) = BridgeThreadItemState_Inference;
  const factory BridgeThreadItemState.skill({
    required String name,
    required String source,
    required String providerId,
    required BridgeSkillResourceBase resourceBase,
    required BridgeSkillActivationCause cause,
    required PlatformInt64 activatedAt,
  }) = BridgeThreadItemState_Skill;
  const factory BridgeThreadItemState.file({
    required String path,
    String? mediaType,
    required PlatformInt64 completedAt,
  }) = BridgeThreadItemState_File;
  const factory BridgeThreadItemState.contextCompaction({
    required BigInt beforeTokens,
    required BigInt afterTokens,
    required PlatformInt64 compactedAt,
  }) = BridgeThreadItemState_ContextCompaction;
}

enum BridgeThreadTextChannel { user, commentary, final_, parentAgent }

class BridgeThreadToolFailure {
  final BridgeThreadToolFailureKind kind;
  final String message;

  const BridgeThreadToolFailure({required this.kind, required this.message});

  @override
  int get hashCode => kind.hashCode ^ message.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadToolFailure &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          message == other.message;
}

enum BridgeThreadToolFailureKind { execution, timedOut, budgetLimited }

class BridgeThreadToolInvocation {
  final String toolCallId;
  final String? callId;
  final String? providerItemId;
  final String name;
  final String arguments;
  final String? workingDirectory;

  const BridgeThreadToolInvocation({
    required this.toolCallId,
    this.callId,
    this.providerItemId,
    required this.name,
    required this.arguments,
    this.workingDirectory,
  });

  @override
  int get hashCode =>
      toolCallId.hashCode ^
      callId.hashCode ^
      providerItemId.hashCode ^
      name.hashCode ^
      arguments.hashCode ^
      workingDirectory.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadToolInvocation &&
          runtimeType == other.runtimeType &&
          toolCallId == other.toolCallId &&
          callId == other.callId &&
          providerItemId == other.providerItemId &&
          name == other.name &&
          arguments == other.arguments &&
          workingDirectory == other.workingDirectory;
}

class BridgeThreadToolOutput {
  final String result;
  final List<BridgeThreadAttachment> attachments;
  final List<String> outputArtifactsJson;
  final int? exitCode;

  const BridgeThreadToolOutput({
    required this.result,
    required this.attachments,
    required this.outputArtifactsJson,
    this.exitCode,
  });

  @override
  int get hashCode =>
      result.hashCode ^
      attachments.hashCode ^
      outputArtifactsJson.hashCode ^
      exitCode.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadToolOutput &&
          runtimeType == other.runtimeType &&
          result == other.result &&
          attachments == other.attachments &&
          outputArtifactsJson == other.outputArtifactsJson &&
          exitCode == other.exitCode;
}

@freezed
sealed class BridgeThreadToolState with _$BridgeThreadToolState {
  const BridgeThreadToolState._();

  const factory BridgeThreadToolState.started() = BridgeThreadToolState_Started;
  const factory BridgeThreadToolState.streaming() =
      BridgeThreadToolState_Streaming;
  const factory BridgeThreadToolState.awaitingApproval() =
      BridgeThreadToolState_AwaitingApproval;
  const factory BridgeThreadToolState.approved() =
      BridgeThreadToolState_Approved;
  const factory BridgeThreadToolState.running({
    required String streamedOutput,
  }) = BridgeThreadToolState_Running;
  const factory BridgeThreadToolState.succeeded({
    required PlatformInt64 completedAt,
    required BridgeThreadToolOutput output,
  }) = BridgeThreadToolState_Succeeded;
  const factory BridgeThreadToolState.failed({
    required PlatformInt64 failedAt,
    required BridgeThreadToolFailure failure,
    BridgeThreadToolOutput? output,
  }) = BridgeThreadToolState_Failed;
  const factory BridgeThreadToolState.denied({
    required PlatformInt64 deniedAt,
    required String reason,
  }) = BridgeThreadToolState_Denied;
  const factory BridgeThreadToolState.cancelled({
    required PlatformInt64 cancelledAt,
    required String reason,
  }) = BridgeThreadToolState_Cancelled;
}
