// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'thread_activity.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

enum BridgeHistoryFault {
  queueFull,
  writeFailed,
  writerUnavailable,
  noProgress,
  checkpointFailed,
  blobFailed,
}

class BridgeThreadActivity {
  final String threadId;
  final String identity;
  final BigInt revision;
  final String turnId;
  final String? inputId;
  final String? attemptId;
  final BridgeThreadActivityKind kind;
  final String summary;
  final bool summaryTruncated;
  final BridgeThreadActivityTools tools;

  const BridgeThreadActivity({
    required this.threadId,
    required this.identity,
    required this.revision,
    required this.turnId,
    this.inputId,
    this.attemptId,
    required this.kind,
    required this.summary,
    required this.summaryTruncated,
    required this.tools,
  });

  @override
  int get hashCode =>
      threadId.hashCode ^
      identity.hashCode ^
      revision.hashCode ^
      turnId.hashCode ^
      inputId.hashCode ^
      attemptId.hashCode ^
      kind.hashCode ^
      summary.hashCode ^
      summaryTruncated.hashCode ^
      tools.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadActivity &&
          runtimeType == other.runtimeType &&
          threadId == other.threadId &&
          identity == other.identity &&
          revision == other.revision &&
          turnId == other.turnId &&
          inputId == other.inputId &&
          attemptId == other.attemptId &&
          kind == other.kind &&
          summary == other.summary &&
          summaryTruncated == other.summaryTruncated &&
          tools == other.tools;
}

/// 活跃工具参数事实的可用程度：命令行摘要在参数不完整或无法提取时回落工具名称。
enum BridgeThreadActivityArguments {
  commandLine,
  opaque,
  streaming,
  unavailable,
}

class BridgeThreadActivityContentPart {
  final String itemId;

  /// canonical item revision；`None` 表示这段内容来自纯内存推导，没有可证明的 revision。
  final BigInt? revision;

  /// 该内容段是否已终态；`false` 表示仍在流式追加。
  final bool complete;
  final String text;

  const BridgeThreadActivityContentPart({
    required this.itemId,
    this.revision,
    required this.complete,
    required this.text,
  });

  @override
  int get hashCode =>
      itemId.hashCode ^ revision.hashCode ^ complete.hashCode ^ text.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadActivityContentPart &&
          runtimeType == other.runtimeType &&
          itemId == other.itemId &&
          revision == other.revision &&
          complete == other.complete &&
          text == other.text;
}

@freezed
sealed class BridgeThreadActivityDetail with _$BridgeThreadActivityDetail {
  const BridgeThreadActivityDetail._();

  const factory BridgeThreadActivityDetail.current({
    required BridgeThreadActivity activity,
    required List<BridgeThreadActivityContentPart> reasoning,
    required List<BridgeThreadActivityContentPart> response,
    required List<BridgeThreadActivityToolDetail> tools,
  }) = BridgeThreadActivityDetail_Current;
  const factory BridgeThreadActivityDetail.superseded({
    required BridgeThreadActivity activity,
    required String requestedActivityId,
  }) = BridgeThreadActivityDetail_Superseded;
  const factory BridgeThreadActivityDetail.ended({
    required String threadId,
    required String activityId,
  }) = BridgeThreadActivityDetail_Ended;
}

enum BridgeThreadActivityKind {
  preparing,
  waitingApi,
  thinking,
  responding,
  planning,
  runningTool,
  awaitingApproval,
  awaitingInput,
  stopping,
}

class BridgeThreadActivityToolDetail {
  final String callId;
  final String? taskId;
  final String name;
  final BridgeThreadActivityToolState state;

  /// canonical item 的原始调用参数；不可读时为 `None`。
  final String? arguments;

  /// 目前观测到的流式或终态输出；暂无输出为 `None`。
  final String? output;
  final BigInt? ordinal;
  final PlatformInt64? startedAt;

  const BridgeThreadActivityToolDetail({
    required this.callId,
    this.taskId,
    required this.name,
    required this.state,
    this.arguments,
    this.output,
    this.ordinal,
    this.startedAt,
  });

  @override
  int get hashCode =>
      callId.hashCode ^
      taskId.hashCode ^
      name.hashCode ^
      state.hashCode ^
      arguments.hashCode ^
      output.hashCode ^
      ordinal.hashCode ^
      startedAt.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadActivityToolDetail &&
          runtimeType == other.runtimeType &&
          callId == other.callId &&
          taskId == other.taskId &&
          name == other.name &&
          state == other.state &&
          arguments == other.arguments &&
          output == other.output &&
          ordinal == other.ordinal &&
          startedAt == other.startedAt;
}

class BridgeThreadActivityToolEntry {
  final String callId;
  final String? taskId;
  final String name;
  final String summary;
  final BridgeThreadActivityArguments arguments;
  final BridgeThreadActivityToolState state;
  final BigInt? ordinal;
  final PlatformInt64? startedAt;

  const BridgeThreadActivityToolEntry({
    required this.callId,
    this.taskId,
    required this.name,
    required this.summary,
    required this.arguments,
    required this.state,
    this.ordinal,
    this.startedAt,
  });

  @override
  int get hashCode =>
      callId.hashCode ^
      taskId.hashCode ^
      name.hashCode ^
      summary.hashCode ^
      arguments.hashCode ^
      state.hashCode ^
      ordinal.hashCode ^
      startedAt.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadActivityToolEntry &&
          runtimeType == other.runtimeType &&
          callId == other.callId &&
          taskId == other.taskId &&
          name == other.name &&
          summary == other.summary &&
          arguments == other.arguments &&
          state == other.state &&
          ordinal == other.ordinal &&
          startedAt == other.startedAt;
}

/// 一条工具调用的执行状态；活动条目只会取到前三个变体，终态调用是 `finished`。
enum BridgeThreadActivityToolState {
  running,
  awaitingApproval,
  cancelling,
  finished,
}

class BridgeThreadActivityTools {
  final int count;

  /// 其中处于后台的活跃工具数量；前台就是工具本身时为 0。
  final int background;
  final List<BridgeThreadActivityToolEntry> active;
  final BridgeThreadActivityToolEntry? latestStarted;

  const BridgeThreadActivityTools({
    required this.count,
    required this.background,
    required this.active,
    this.latestStarted,
  });

  static Future<BridgeThreadActivityTools> default_() => RustLib.instance.api
      .crateApiStudioTypesThreadActivityBridgeThreadActivityToolsDefault();

  @override
  int get hashCode =>
      count.hashCode ^
      background.hashCode ^
      active.hashCode ^
      latestStarted.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadActivityTools &&
          runtimeType == other.runtimeType &&
          count == other.count &&
          background == other.background &&
          active == other.active &&
          latestStarted == other.latestStarted;
}

enum BridgeThreadStorageExecution { running, pausing, paused }

/// Thread 存储状态；所有字段都是 typed 事实，缺失表示未知而不是零。
class BridgeThreadStorageState {
  final BridgeHistoryFault? fault;
  final BigInt faultGeneration;
  final BigInt? acceptedSequence;
  final BigInt? durableSequence;
  final BridgeThreadStorageExecution execution;
  final bool pressurePaused;

  /// 硬故障是否仍闩住新准入，必须由用户显式 `resume_thread_history` 继续。
  ///
  /// `pressure_paused` 是自动恢复的背压；本字段为 `true` 时重试保存成功也不会自动继续。
  final bool resumeRequired;

  /// 现在显式继续是否真的会被后端接受。
  ///
  /// `resume_required` 只说明故障闩住了准入；本字段还要求恢复已被核验（保存重试的目标水位已
  /// durable、没有仍在报告的硬故障、没有未上交批次、也没有字节阈值挡住）。UI 只能用本字段启用
  /// “继续”；后端 resume 仍会重查，所以它不是前端推断。
  final bool canResume;

  /// core 提供的原始错误文本；它不是故障类别的来源。
  final String? lastError;

  const BridgeThreadStorageState({
    this.fault,
    required this.faultGeneration,
    this.acceptedSequence,
    this.durableSequence,
    required this.execution,
    required this.pressurePaused,
    required this.resumeRequired,
    required this.canResume,
    this.lastError,
  });

  @override
  int get hashCode =>
      fault.hashCode ^
      faultGeneration.hashCode ^
      acceptedSequence.hashCode ^
      durableSequence.hashCode ^
      execution.hashCode ^
      pressurePaused.hashCode ^
      resumeRequired.hashCode ^
      canResume.hashCode ^
      lastError.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BridgeThreadStorageState &&
          runtimeType == other.runtimeType &&
          fault == other.fault &&
          faultGeneration == other.faultGeneration &&
          acceptedSequence == other.acceptedSequence &&
          durableSequence == other.durableSequence &&
          execution == other.execution &&
          pressurePaused == other.pressurePaused &&
          resumeRequired == other.resumeRequired &&
          canResume == other.canResume &&
          lastError == other.lastError;
}
