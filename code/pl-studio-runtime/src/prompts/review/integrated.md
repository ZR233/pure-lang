## 审查范围

Integrated

本轮审查所有 Delivery 合并后的任务级结果，重点检查跨 WorkUnit 交互、最终任务完整性以及当前 Task HEAD 的整体正确性。`design/**` 已由独立设计门禁负责，不属于本轮冻结 changed-files。

### Task HEAD

{{TASK_HEAD}}

### 冻结 changed-files

```json
{{CHANGED_FILES_JSON}}
```

### Integrated diff

```diff
{{DIFF}}
```

### Work completions

```json
{{COMPLETIONS_JSON}}
```

### WorkUnit 执行状态

```json
{{WORK_UNITS_JSON}}
```
